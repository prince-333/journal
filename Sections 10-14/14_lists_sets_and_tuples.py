'''

Collecition - single "variable" used to store multiple values

List = [] ordered and changeable. Duplicates OK

Set = {} unordered and immutable, but Add/Remove OK. NO duplicates.

Tuple = () ordered and unchangeable. Duplicates OK. FASTER

'''

fruits = ['apple', 'orange', 'banana', 'coconut']

'''

List

'''
# print(fruits[::-1])                 Reads list backwards
# print(len(fruits))                  How many steps in the list
# print('apple' in fruits)            Finds the string and returns as a boolean statement
 
# fruits[0] = 'pineapple'             Replaces one step in the list
# fruits.append('pineapple')          Adds a step at the end of the list.
# fruits.remove('apple')              Removes apple
# fruits.insert(0, 'pineapple')       Adds a step at a specific step.
# fruits.sort()  
# fruits.reverse()                    Reads list backwards
# fruits.clear()                      Removes the list
# print(fruits.index('coconut'))      Finds the index of the string
# print(fruits.count('banana'))         Counts how much times a string appears in the list.

'''

Set

'''



print(fruits)

#for fruit in fruits:
    #print(fruit)