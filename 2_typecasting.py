# Typecasting = the process of converting a variable from one data type to another,
#               e.g., (string, integer, float, and boolean)

name = "Prince"
age = 25 
gpa = 3.2
is_student = True

#Convert gpa to float (Take off the #)
#gpa = int(gpa)

#print(gpa)

#Convert age to float (Take off the #)
#age = float(age)

#print(age)

age = str(age)

age += "1"

print(age)

#When Converting  rom int to string, make sure to add, "" to it, indiciting its a string, 
